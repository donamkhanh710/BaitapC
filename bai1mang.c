#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a[10];
    for(int i=0; i<10; i++){
        printf("phan tu thu %d la : ", i+1);
        scanf("%d", &a[i]);
}for(int i=0; i<10; i++)
printf("%d  ", a[i]);
}
